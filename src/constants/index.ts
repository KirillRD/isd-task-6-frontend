export const PAGE_SIZE = 7;
export const SCHEDULER_DELAY = 5000;
export const TOAST_LIFE_TIME = 2500;

export enum ApiRoute {
  DEFAULT = '/',
  LOGIN = '/login',
  MAIL = '/mail',
  NOT_FOUND = '/not-found'
}
